# Dependencies
# /node_modules* cubre tambien node_modules.nosync y los duplicados "node_modules 2" de iCloud
/node_modules*
/.pnp
.pnp.js
yarn.lock
pnpm-lock.yaml
# package-lock.json SI se commitea: sin el, Vercel resuelve dependencias frescas
# en cada build y el deploy puede romperse sin que nadie toque codigo.

# Testing
/coverage

# Next.js
# /.next* cubre .next, .next.nosync y los duplicados ".next 2" de iCloud
/.next*
/out/
*.rsc

# Production
/build
/dist

# Misc
.DS_Store
*.pem
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Local env files
.env*.local
!.env.example

# Vercel
.vercel

# IDE
.vscode
.idea
*.swp
*.swo
*~
.sublime-project
.sublime-workspace

# OS
Thumbs.db
.AppleDouble
.LSOverride

# Claude Code
.claude/skills/**/node_modules
.claude/plugins
*.mcp.json
!example.mcp.json

# Supabase
supabase/.branches
supabase/.temp

# Local Claude settings (per-user, not shared)
.claude/settings.local.json

# Playwright MCP cache / artifacts
.playwright-mcp/

# Screenshots and snapshots (ephemeral, not code)
/*.png
/*.jpeg
/*.jpg
/tasks-*.md
/tasks-snapshot.md

# Media grande — va a Bunny/CDN, NUNCA a git
*.mov
*.MOV
*.mp4
*.webm
# assets/ entero es media local (regla: "media va a assets/, gitignored")
/assets/

# Backup files
*.bak

# Local content / personal media
/Contenidos/
tsconfig.tsbuildinfo

# Remotion source clips (heavy, no commit)
/public/raw/

# Transcripts privados de reuniones (no commitear)
Update_task_adrian_y_marco
*.transcript
/transcripts/
